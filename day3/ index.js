// Membaca kode

let result = 0;
for (let i = 0; i < numbers.length; i++) {
  result += numbers[i];
}
// Niat mau ngapain > ngumpulin nilai
// Identifikasi peran variabel > result=penampung. i=petunjuk, numbers=sumber data
// Siapa yang nilainya bergerak > counter, state, accumulator
// Cari batas(scope) > hidup dan mati dimana > kalo jelas=aman
// Cara baca > baca pelan, lompat-lompat, ulang ke atas